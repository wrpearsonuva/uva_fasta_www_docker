services:
  web:
    container_name: fasta_www3
    build:
      context: docker/Dockerfile

    volumes:
      - ./slib2:/slib2
      - ./log/web:/var/log/nginx/web
      - ./certs:/etc/nginx/certs:ro

    ports:
      - "80:80"
      - "443:443"
